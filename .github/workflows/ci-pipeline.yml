name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    services:
      docker:
        image: docker:20.10.16
        options: --privileged

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up .NET environment
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Install Docker Compose
        run: |
          sudo curl -L "https://github.com/docker/compose/releases/download/v2.17.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          sudo chmod +x /usr/local/bin/docker-compose

      - name: Build and run unit tests
        run: |
          dotnet build CardValidation.sln --configuration Release
          dotnet test UnitTests/UnitTests.csproj --no-build --logger trx --results-directory ./test-results/unit

      - name: Build Docker images
        run: |
          docker build -t card-validation-api -f CardValidation.Web/Dockerfile .
          docker build -t card-validation-integration-tests -f Dockerfile.integration-tests .

      - name: Run integration tests with Docker Compose
        run: docker-compose up --abort-on-container-exit --build

      - name: Upload test results
        uses: actions/upload-artifact@v3
        with:
          name: Unit-Test-Results
          path: ./test-results/unit/*.trx

      - name: Upload integration test results
        if: always()
        run: |
          mkdir -p ./test-results/integration
          docker cp $(docker ps -qf "name=integration-tests"):/app/IntegrationTests/TestResults ./test-results/integration

        uses: actions/upload-artifact@v3
        with:
          name: Integration-Test-Results
          path: ./test-results/integration/**/*.trx
